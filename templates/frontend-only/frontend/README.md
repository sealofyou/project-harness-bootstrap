# 纯前端模板

这是 `frontend-only` 模板生成的前端占位目录。

## 使用原则

- 优先按用户指定框架落地，例如 React、Next.js、Vue
- 如果用户未指定框架，先在项目规约中记录为待确认
- 保留 Harness 的测试、验收、回写、闸门机制

## 推荐后续动作

1. 确认前端框架
2. 初始化实际前端工程
3. 将运行、构建、测试命令回写到 `docs/specs/05-测试与验证策略.md`
